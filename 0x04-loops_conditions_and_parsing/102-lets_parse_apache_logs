#!/usr/bin/env bash
# This script parses the Apache log file and displays the visitor IP and HTTP status code.

# Use awk to extract the IP (first field) and HTTP status code (9th field) from the log
awk '{print $1, $9}' apache-access.log

